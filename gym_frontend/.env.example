# Backend API URL - will be set by deployment agent
REACT_APP_API_URL=http://localhost:3001

# Site URL for redirects - will be mapped to deployment URL by deployment agent
REACT_APP_SITE_URL=http://localhost:3000

# Disable host checking for development server (set to true for cloud/preview environments)
# DANGEROUSLY_DISABLE_HOST_CHECK=true

# WebSocket configuration for hot reload in cloud environments
# WDS_SOCKET_HOST=your-preview-host.com
# WDS_SOCKET_PORT=3000
# WDS_SOCKET_PATH=/ws
